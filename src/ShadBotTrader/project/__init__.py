"""Project Intelligence Platform (PIP).

The subsystem that lets ShadBotTrader understand itself: it scans the
workspace, analyses source code and git state, and generates the
canonical project-state artifacts under ``project_state/generated/``.
"""
