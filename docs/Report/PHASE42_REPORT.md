# فاز ۴۲ — چرا هنوز هیچ epoch‌ای نمی‌دیدی

**تاریخ:** 2026-08-17
**وضعیت:** ✅ کامل — Quality Gate سبز

---

## این بار خطوط تولید می‌شدند — و دور ریخته می‌شدند

فاز ۳۶ و ۳۹ گزارشگر را وصل کردند و واقعاً کار می‌کرد. مشکل **آخرین قدم**
بود: خطوط تولید می‌شدند، به فایل لاگ می‌رسیدند، و بعد قبل از رسیدن به تو
حذف می‌شدند.

دو علت مستقل.

### باگ ۳۴ — سیل خطوط batch، نتایج را بیرون می‌انداخت

هر batch یک خط می‌نوشت. روی دیتای واقعی یعنی صدها خط در هر epoch.
داشبورد فقط **۲۰۰ خط آخر** را می‌خواند:

```
اندازه‌گیری روی یک اجرای کوچک:
  کل خطوط batch در لاگ : 316
  از ۲۰۰ خطی که صفحه می‌دید:
     خطوط batch : 158
     خطوط epoch : 3
```

روی دیتاست بزرگ‌تر تو، ۲۰۰ خط آخر **کلاً** خطوط batch بودند و هیچ epoch‌ای
باقی نمی‌ماند. شمارنده جلو می‌رفت، عدد دقت هرگز دیده نمی‌شد.

### باگ ۳۵ — ترفند `\r` از داخل pipe رد نمی‌شود

خط پیشرفت batch با `\r` نوشته می‌شد تا در جا به‌روز شود. این فقط روی
**ترمینال** کار می‌کند. وقتی خروجی به یک pipe و بعد به فایل می‌رود، `\r`
فقط یک کاراکتر است — پس هر «به‌روزرسانی» یک خط دائمی جدید می‌شد. دقیقاً
همین سیل باگ ۳۴ را چند برابر بدتر می‌کرد.

---

## رفع

**۱. حداکثر ۸ خط پیشرفت در هر epoch**، با فاصلهٔ یکنواخت، و همیشه خط آخر
(۱۰۰٪) — چون تمام‌شدن روی ۹۴٪ شبیه گیرکردن است.

```
316 خط batch  →  60 خط
```

**۲. `\r` کاملاً حذف شد.** حالا خطوط عادی‌اند که از خواندن فایل جان سالم
به در می‌برند.

**۳. پنجرهٔ ۲۰۰ خطی داشبورد هوشمند شد.** وقتی لاگ بزرگ‌تر از پنجره است،
اول خطوط **نتیجه** نگه داشته می‌شوند (epoch، fold، val_loss، SAVED، خطاها)
و خطوط batch رقیق می‌شوند — نه برعکس. تازه‌ترین خطوط همیشه می‌مانند.

---

## آنچه حالا می‌بینی

```
fold   1/2 | train[0:412] (412 samples) -> val[412:421] (9 samples)
    [--------------------]   1.9% | batch 1/52 | loss 0.3541 | mae 0.0348
    [#####---------------]  25.0% | batch 13/52 | loss 0.2135 | mae 0.0173
    [##########----------]  48.1% | batch 25/52 | loss 0.1818 | mae 0.0137
    [###############-----]  71.2% | batch 37/52 | loss 0.1659 | mae 0.0114
    [####################] 100.0% | batch 52/52 | loss 0.1543 | mae 0.0094
  epoch 1/3 | loss 0.1543 | val_loss 0.1209 | lr 1.50e-04
  epoch 2/3 | loss 0.1146 | val_loss 0.1088 | lr 1.50e-04
  epoch 3/3 | loss 0.1045 | val_loss 0.1001 | lr 1.50e-04
```

### تأیید زنده از خود HTTP داشبورد

```
[12s] busy=True lines=46  epochs=1
[24s] busy=True lines=81  epochs=4
[36s] busy=True lines=148 epochs=7
[48s] busy=False (finished)
```

خطوط epoch **حین اجرا** رشد می‌کنند — همان چیزی که ندیدی.

---

## تأیید

```
black ✅  ruff ✅  mypy (293 files) ✅
pytest 1354 passed, 12 skipped   (قبلاً 1340)
RUN_TF=1 ai + progress: 124 passed
```
**۱۴ تست جدید**، از جمله تستی که ثابت می‌کند با ۲٬۴۰۰ خط batch، **هر شش
خط epoch** هنوز دیده می‌شوند.

---

## نکته دربارهٔ لاگ پاورشل تو

آن هشدارهای TensorFlow که در کنسول داشبوردت می‌بینی (`oneDNN custom
operations are on`) طبیعی‌اند — پیام‌های راه‌اندازی TF هستند که به
`stderr` می‌روند. خروجی واقعی آموزش در **صفحهٔ وب** و در فایل
`run_logs/train_dual_models.log` است، نه در آن کنسول.

فایل لاگ را هم می‌توانی مستقیم باز کنی:
```powershell
Get-Content run_logs\train_dual_models.log -Wait -Tail 40
```

---

## بدهی صریح

- **۸ خط در هر epoch** یک انتخاب است نه یک قانون. اگر برای دیتای بزرگ کم
  بود، `BATCH_LINES_PER_EPOCH` قابل تنظیم است.
- پنجرهٔ ۲۰۰ خطی هنوز ثابت است؛ برای اجراهای خیلی طولانی شاید بهتر باشد
  کاربر بتواند بزرگش کند.
