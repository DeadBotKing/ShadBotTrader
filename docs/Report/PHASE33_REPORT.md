# فاز ۳۳ — پیوستگی و آپدیت افزایشی دیتاست

**سؤال تو سه بخش داشت. جواب هر سه ❌ بود** — و با آزمایش ثابتش کردم، نه حدس.

---

## ۱. آنچه واقعاً اتفاق می‌افتاد

```
ingest #1 : v1  candles stored=200
ingest #2 : v2  candles stored=50     ← ۲۰۰ کندل قبلی نابود شد
```

| انتظار تو | واقعیت قبل |
|---|---|
| دیتای جدید **اضافه** شود | ❌ نسخهٔ جدید می‌ساخت، `query` فقط آخرین را می‌خواند |
| بیش از ۱۰۰k → **۱۰۰k آخر** بماند | ❌ مفهوم سقف اصلاً وجود نداشت |
| **پیوستگی زمانی** چک شود | ❌ `QualityAnalyzer` گپ را فقط داخل یک نسخه می‌دید |

نگرانی‌ات درست بود — این دقیقاً همان چیزی است که مدل را بی‌صدا خراب می‌کند.

---

## ۲. سخت‌ترین بخش: گپ همیشه گپ نیست

بازار تعطیل می‌شود. «شنبه کندل نیست» عادی است؛ «سه‌شنبه کندل نیست» یعنی
دیتای گم‌شده. بررسی ساده‌لوحانه یا هر آخر هفته را هشدار می‌دهد، یا هر حفرهٔ
واقعی را از دست می‌دهد.

### تقویم **یاد گرفته می‌شود**، نه اعلام

از تاریخچهٔ موجود:

```
Mon (10,10)  Tue (10,10)  Wed (10,10)  Thu (10,10)  Fri (10,10)
Sat  (0,10)  Sun  (0,10)          →  closed on Sat, Sun
```

تقویم هاردکد برای کریپتو غلط است، برای بروکری در timezone دیگر غلط است، و
ظرف یک سال کهنه می‌شود. تقویم یادگرفته‌شده خودش با هر ابزار و هر بروکری وفق
می‌دهد — و **شواهدش قابل بازرسی است**.

### نتیجه روی سناریوی دقیق تو

آخرین کندل ذخیره‌شده: **سه‌شنبه ۹ ژوئیه**

```
روز کاری بعدی (چسبیده)        → ✅ بدون شکاف
۳ روز بعد                     → ❌ ۲ کندل گم
یک ماه بعد                    → ❌ ۲۱ کندل گم
```

**۲۱ نه ۳۰** — آخر هفته‌ها را نشمرد. دقیقاً همان چیزی که خواستی.

---

## ۳. خط لولهٔ آپدیت

```
تاریخچهٔ ذخیره‌شده را بخوان
  → کندل جدید بگیر
  → محل اتصال را چک کن (روزهای تعطیل حساب نمی‌شوند)
  → اگر شکاف بود، از بروکر backfill کن
  → merge، حذف تکراری، نگه‌داشتن N تای آخر
  → پیوستگی نتیجه را تأیید کن
  → بنویس
```

### سه قاعدهٔ سخت

**۱. تأیید قبل از نوشتن.** آپدیت ناموفق باید دیتاست قبلی را **دست‌نخورده**
بگذارد. نوشتن اول و اعتبارسنجی بعد، هر بار شکست یک دیتاست خراب به جا می‌گذارد.

**۲. کندل جدید در تصادم برنده است.** کندل ۱H جاری قبل از بسته‌شدن بارها
خوانده می‌شود؛ آخرین خواندن درست‌ترین است.

**۳. رد کردن پیش‌فرض است.** چسباندن دو سر یک حفره، به مدل حرکت قیمتی یاد
می‌دهد که **هرگز رخ نداده** — و هیچ تستی بعدش نمی‌گیردش.

---

## ۴. تأیید عملی

```
۱) اول            : 50 کندل
۲) append ۲۰تا     : اضافه 20 → مجموع 60 (۱۰ قدیمی حذف)  ✅ سقف
۳) همان دیتا دوباره: اضافه 0، آپدیت 20                    ✅ بدون تکرار
۴) پرش یک‌ماهه     : REFUSED — دیتاست دست‌نخورده ماند       ✅
۵) با backfill     : ۲۱ کندل وصله شد → پیوسته              ✅
```

خروجی واقعی backfill:
```
stored before : 40
fetched       : 10
backfilled    : 21 (gap repaired)
new candles   : 31
stored now    : 71
continuity    : OK — no gaps
```

---

## ۵. در GUI

دکمهٔ **Fetch market data** حالا دو ورودی تازه دارد:

| ورودی | معنی |
|---|---|
| **Keep at most** | سقف چرخشی (پیش‌فرض ۱۰۰٬۰۰۰) |
| **Allow gap** | `1` = شکافی که بروکر نتوانست پر کند را عمداً بپذیر |

و به‌جای «Ingested ...» گزارش کامل می‌دهد:
```
XAUUSD 5M: +1,240 new, 100,000 stored
  stored before : 98,760
  fetched       : 5,000
  new candles   : 1,240
  updated bars  : 3,760
  dropped       : 0 oldest
  continuity    : OK — no gaps
```

اگر شکاف پر نشود، **رد می‌کند** و می‌گوید چرا:
```
REFUSED: 21 candle(s) are missing between the stored history and the
new data. Joining them would teach the model a price move that never
happened.
  The stored dataset was NOT modified.
```

---

## ۶. 🐞 باگ فرعی که تست خودم گرفت

`next_version` دایرکتوری **raw** را می‌شمرد، ولی سرویس در **normalized**
می‌نوشت → دومین آپدیت با `FileExistsError` می‌ترکید. رفع شد.

---

## ۷. کیفیت

```
black ✅  ruff ✅  mypy (285 files) ✅
pytest  1155 passed, 12 skipped     (قبلاً 1101)
```

**۵۴ تست جدید:** ۳۳ پیوستگی و تقویم · ۲۱ آپدیت افزایشی

از جمله: آخر هفته گپ نیست، پرش یک‌ماهه رد می‌شود، آپدیت ردشده دیتاست را
تغییر نمی‌دهد، backfill دقیقاً بازهٔ گم‌شده را می‌خواهد، و بروکر بدون
تاریخچه همچنان رد می‌شود.

---

## ۸. صادقانه

- **کندل گم‌شده ساخته نمی‌شود.** درون‌یابی قیمتی که بازار چاپ نکرده، بدتر از
  یک رد صادقانه است.
- **تقویم تعطیلات رسمی جداگانه ندارد.** تقویم یادگرفته‌شده از دیتای خود
  بروکر می‌آید — تنها مرجعی که اینجا اهمیت دارد. اگر بروکر روز کریسمس دیتا
  نداشته باشد و آن روز در تاریخچه باشد، خودش یاد می‌گیرد.
- **`tolerance` پیش‌فرض ۲ است** — بروکرها گاهی یک بار را می‌اندازند و
  متوقف‌کردن هر آپدیت به‌خاطر آن، سود واقعی ندارد.
