# فاز ۳۴ — چارت شمعی و بازرسی دیتاست

**درخواست تو:** چارت کندلی ببینم، و بدانم دیتاست چند کندل دارد و چه ستون‌هایی —
برای هر سهٔ `Fetch market data`، `Update features`، `Build training dataset`.

**نتیجه:** یک صفحهٔ واحد در `/data` که هر سه را نشان می‌دهد.

---

## ۱. چطور ببینی

```powershell
python -m ShadBotTrader.dashboard_cli --db shadbot.db serve
```

بعد در مرورگر:
- `http://localhost:8080/data` — یا لینک **inspect the data** بالای داشبورد
- `http://localhost:8080/api/data` — همان چیز به‌صورت JSON

بعد از هر یک از آن سه دکمه، در نتیجه هم نوشته می‌شود: `See the candles: open /data`

---

## ۲. سه بخش، سه منبع

| بخش | منبع | جواب کدام دکمه |
|---|---|---|
| **Candles** | `ParquetCandleStore` | Fetch market data |
| **Computed features** | `ParquetFeatureStore` | Update features |
| **Training matrix** | `TrainingDataService` | Build training dataset |

---

## ۳. چارت شمعی

- شمع سبز = close بالای open، قرمز = پایین
- نوار **حجم** زیر نمودار (قابل خاموش کردن)
- پنجرهٔ قابل انتخاب: ۶۰ / ۱۲۰ / ۲۰۰ / ۳۰۰ کندل
- نوار آمار زنده: تعداد نمایش‌داده‌شده، آخرین close، سقف، کف، تغییر و درصد

**یک تصمیم مهم:** چارت حداکثر ۳۰۰ کندل می‌کشد، ولی **تعداد کل را کامل گزارش
می‌کند**. یک دیتاست ۱۰۰ هزارتایی نباید به صفحهٔ وب ۱۰۰ هزار نقطه‌ای تبدیل شود
— ولی کوتاه‌کردن نمودار و دروغ‌گفتن دربارهٔ تعداد کل، از هر دو بدتر بود.

خروجی واقعی:
```
CANDLES : 1,500 stored | chart 300 pts | 1955.05-1987.99
          continuous=True | market trades every day
```

---

## ۴. اطلاعات دیتاست

### کندل‌ها
تعداد ذخیره‌شده · اولین و آخرین زمان · بازهٔ روز · **وضعیت پیوستگی** (از فاز ۳۳) ·
تقویم یادگرفته‌شدهٔ بازار

### ماتریس آموزش
```
MATRIX  : 1,397 rows x 123 cols | digest c72902b496ab8270
          {'raw price': 8, 'candle shape': 6, 'feature': 109}
          constant cols: ['close_rel']
```

**هر ستون** با این جزئیات فهرست می‌شود:

| ستون | معنی |
|---|---|
| **Kind** | raw price / candle shape / feature / target |
| **Coverage** | چند درصد سطرها مقدار معتبر دارند (با نوار بصری) |
| **Min / Max** | بازهٔ مشاهده‌شده |
| **Latest** | آخرین مقدار |
| **Flags** | `gaps` اگر ناقص · `constant` اگر هرگز تغییر نکند |

**چرا `constant` را علامت می‌زنم:** ستونی که هرگز تغییر نکند به مدل هیچ چیز
یاد نمی‌دهد. `close_rel` طبق ساختار همیشه صفر است — صفحه این را **می‌گوید**
به‌جای اینکه بگذارد مثل یک ورودی واقعی به‌نظر برسد.

**نمونه‌برداری:** ستون‌ها با گام (تا ۲۰۰۰ سطر) خلاصه می‌شوند تا ماتریس
۱۰۰k×۱۲۳ فوری باز شود — ولی تعداد کل سطرها صادقانه کنارش گزارش می‌شود.

### فیچرها
```
FEATURES: 109 stored | ['atr_14', 'atr_tr_10', 'atr_tr_15', 'atr_tr_20']
```
هر فیچر با آخرین نسخه، تعداد نسخه‌ها و حجم فایل.

---

## ۵. فقط می‌خواند، محاسبه نمی‌کند

`DataInspector` یک **Gateway** به‌معنای فاز ۱۹ است: از انبارها می‌خواند و
برای نمایش شکل می‌دهد. **هر عددی روی این صفحه از storage خوانده شده.** اگر
صفحه و یک ران با هم اختلاف داشتند، ران اشتباه است — صفحه نمی‌تواند جواب
دیگری بسازد.

انبار خالی یا ناخوانا → نتیجهٔ خالی، نه exception. داشبوردی که چون هنوز چیزی
fetch نشده crash کند، دقیقاً وقتی بی‌فایده است که اپراتور می‌خواهد همین را
بفهمد.

اگر هیچ چیز ذخیره نشده باشد، صفحه می‌گوید کدام دکمه را بزنی.

---

## ۶. کیفیت

```
black ✅  ruff ✅  mypy (287 files) ✅
pytest  1182 passed, 12 skipped     (قبلاً 1155)
```

**۲۷ تست جدید** — شمارش کندل، محدودیت چارت بدون دروغ در تعداد، تشخیص گپ،
توصیف ستون‌ها، علامت‌گذاری ستون ثابت، صفحهٔ خالی، و مسیر HTTP.

JavaScript چارت هم با Node اجرا و اعتبارسنجی شد.

---

## ۷. فایل‌ها

**جدید**
```
docs/Phases/Phase34.md
src/ShadBotTrader/presentation/gateway/data_inspector.py
src/ShadBotTrader/presentation/web/data_renderer.py
tests/integration/test_data_inspector.py
PHASE34_REPORT.md
```

**ویرایش‌شده:** `server.py` (مسیر `/data` و `/api/data`)، `renderer.py`
(لینک هدر)، `handlers.py` (اشاره به `/data` در نتیجهٔ دکمه‌ها)

---

## ۸. صادقانه: چه چیزی نساختم

- **زوم و pan ندارد.** انتخاب پنجره نیاز را پوشش می‌دهد؛ یک کتابخانهٔ چارت
  کامل، وابستگی‌ای است که این صفحه توجیهش نمی‌کند.
- **اندیکاتور روی چارت نمی‌کشد.** مقادیر فیچرها عددی فهرست می‌شوند؛ کشیدن
  ۱۰۹ تای آن‌ها روی شمع‌ها ناخوانا می‌شد.
- **قابل ویرایش نیست** — مثل خود داشبورد، کاملاً read-only.
