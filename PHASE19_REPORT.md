# گزارش Phase 19 — GUI / Dashboard

**تاریخ:** ۲۰۲۶-۰۸-۱۶
**مرجع معماری:** `docs/Phases/Phase19.md`

---

## چه چیزی ساخته شد

یک داشبورد وب **فقط-خواندنی** روی حالتی که Sprint P8 پایدار کرد.

```
View  ->  ViewModel  ->  Gateway  ->  Application / Infrastructure
```

### `src/ShadBotTrader/presentation/`

| فایل | نقش |
|---|---|
| `viewmodels/models.py` | ۱۰ ViewModel تغییرناپذیر + توابع قالب‌بندی |
| `gateway/dashboard_gateway.py` | تنها درِ ورود GUI به داده |
| `web/renderer.py` | تولید HTML (CSS و SVG درون‌خطی) |
| `web/server.py` | سرور stdlib، فقط GET |
| `dashboard_cli.py` | `serve` / `export` / `show` / `state` |

### پنل‌ها

پرتفوی و پوزیشن‌ها · نمودار جریان نقدی محقق‌شده · ردّ حسابرسی تصمیم‌ها ·
تاریخچه‌ی اجرا · حافظه‌ی یادگیری · دلایل رد معاملات · فهرست جلسات ·
سلامت دیتابیس

---

## مرز معماری — ساختاری، نه توافقی

فاز ۱۹ §۴ می‌گوید GUI نباید سفارش اجرا کند، مدل آموزش دهد یا حالت را
تغییر دهد. به‌جای نوشتن این در مستندات، **غیرممکنش کردم**:

| تضمین | نحوه‌ی اعمال |
|---|---|
| Gateway هیچ متد تغییردهنده ندارد | تستی که نام همه‌ی متدهای عمومی را بررسی می‌کند |
| ViewModelها تغییرناپذیرند | `@dataclass(frozen=True)` + تست `FrozenInstanceError` |
| ViewModel به دامنه/زیرساخت دسترسی ندارد | تستی که با `ast` ایمپورت‌ها را پارس می‌کند (§۱۰) |
| سرور فقط GET می‌پذیرد | `POST`/`PUT`/`DELETE`/`PATCH` → **405** با توضیح |
| هیچ endpoint معاملاتی وجود ندارد | تست: `/trade`، `/order`، `/execute` → ۴۰۴ |

اگر روزی کسی متدی به Gateway اضافه کند که چیزی را تغییر دهد، تست می‌شکند.

---

## بدون وابستگی جدید

با `http.server` استاندارد ساخته شد. CSS، نمودار SVG و همه‌چیز درون‌خطی
است — بدون script، بدون فونت، بدون CDN.

نتیجه: صفحه در preview جعبه‌شنی، فایل ایمیل‌شده یا مرورگر بدون اینترنت
یکسان رندر می‌شود. `--export` یک فایل کاملاً مستقل تولید می‌کند.

---

## 🐞 دو باگ که پیدا و رفع شدند

### ۱. حالت ذخیره‌شده نامرئی می‌شد

`is_empty` فقط `sessions` و `candidates` را بررسی می‌کرد. دیتابیسی که
**پوزیشن واقعی** داشت ولی تصمیمی ژورنال نشده بود، صفحه‌ی «هنوز چیزی
ثبت نشده» را نشان می‌داد و داده‌ی واقعی پنهان می‌شد.

تست XSS این را گرفت — نه به خاطر escaping (که درست کار می‌کرد) بلکه چون
پنل اصلاً رندر نشده بود. یک تست ناموفق، باگ دیگری را لو داد.

**رفع:** پرتفوی و اجراها هم در محاسبه‌ی خالی‌بودن لحاظ شدند. سه تست
رگرسیون اضافه شد.

### ۲. عدد نگهبان به کاربر نشان داده می‌شد

`RiskAdjustedObjective` برای اجرای کم‌معامله **-۱٬۰۰۰٬۰۰۰** برمی‌گرداند —
یک نشانگر به معنای «شواهد کافی نیست». داشبورد آن را عیناً چاپ می‌کرد:

```
in=-1000000.0000 out=-1000000.0000
```

این یعنی «فاجعه‌ی مالی»، در حالی که معنایش «قابل قضاوت نیست» است.

**رفع:** حالا `insufficient` نمایش داده می‌شود. دو تست رگرسیون اضافه شد.

> این همان الگویی است که در Sprint P7 هم دیدیم: نگهبان‌ها نباید مثل
> اندازه‌گیری رفتار کنند.

---

## خروجی نمونه

```
--- Portfolio (demo) ---
  net cash flow  : 111.19 USD
  realised PnL   : +112.00
  fees           : 0.8120
  net realised   : +111.19

--- Decisions (latest 4) ---
  XAUUSD_i  exit   conf=0.940  risk=pass
  XAUUSD_i  hold   conf=0.880  risk=—
  XAUUSD_i  hold   conf=0.200  risk=—     no_signal
  XAUUSD_i  enter  conf=0.900  risk=pass
```

---

## وضعیت کیفیت

| بررسی | نتیجه |
|---|---|
| `black --check .` | ✅ ۳۲۹ فایل |
| `ruff check .` | ✅ پاس |
| `mypy src` | ✅ ۲۴۴ فایل |
| `pytest` | ✅ **۶۰۶ passed, 6 skipped** |
| `RUN_TF=1 pytest` | ✅ **۶۱۲ passed** |
| ۹ دمو × ۲ بار | ✅ همه OK |

**رشد تست‌ها:** ۵۷۰ → **۶۱۲** (۴۲ تست جدید)

- ۲۸ تست واحد (قالب‌بندی، gateway، renderer، مرز فقط-خواندنی)
- ۱۴ تست یکپارچگی HTTP (مسیرها، content-type، رد شدن متدهای تغییردهنده)

---

## دستورات جدید

```bash
python scripts/run_dashboard.py                    # داده‌ی نمونه + سرور
python scripts/run_dashboard.py --export out.html  # فایل مستقل
python scripts/run_dashboard.py --no-seed --db shadbot.db

shadbot-dashboard serve --db shadbot.db --port 8080
shadbot-dashboard show
shadbot-dashboard state
shadbot-dashboard export --out report.html
```

---

## آنچه عمداً ساخته نشد

- **کنترل از داشبورد** — دکمه‌ی «اجرای بک‌تست» یا «شروع معامله» نیست.
  فاز ۱۹ این را منع کرده و اضافه کردنش تمام تضمین‌های بالا را باطل می‌کند.
- **به‌روزرسانی زنده (WebSocket/SSE)** — نیاز به وضعیت در حال اجرا دارد؛
  فعلاً refresh صفحه کافی است.
- **نمودار منحنی سرمایه‌ی هر کندل** — equity هر بار ذخیره نمی‌شود، پس
  نموداری که رسم می‌شود از تراکنش‌های واقعی می‌آید. **منحنی تخمینی
  نساختم**؛ صفحه صراحتاً می‌گوید چه چیزی نشان داده نمی‌شود.
- **احراز هویت** — یک viewer محلی است. اگر روزی در شبکه قرار گرفت،
  احراز هویت لازم می‌شود.

---

## به‌روزرسانی: دکمه‌های اجرا اضافه شدند

### چرا معماری اجازه می‌دهد

فاز ۱۹ §۳ صراحتاً **Command Dispatch** را جزو مسئولیت‌های GUI آورده، و
§۱۲-۱۳ مسیرش را تعریف کرده‌اند:

```
Controller -> Command Bus -> Command Handler -> Application Service
```

حتی نمونه‌هایش را نام برده: `TrainModelCommand`، `RefreshMarketCommand`.

پس دکمه‌ها نقض §۴ نیستند — به شرطی که GUI فقط **قصد** بفرستد و کار را
سرویس‌های اپلیکیشن انجام دهند. دقیقاً همین پیاده شد.

### هفت دکمه

| دکمه | کاری که می‌کند |
|---|---|
| Fetch market data | داده‌ی واقعی از متاتریدر ۵ |
| Update features | محاسبه‌ی مجدد فیچرها + ثبت در دیتابیس |
| Retrain the model | آموزش مجدد WaveNet |
| Run a backtest | بک‌تست روی کندل‌های ذخیره‌شده |
| Run optimisation | جستجوی پارامتر با دروازه‌ی ارتقا |
| Run a trading cycle | یک تصمیم + اجرا روی آخرین کندل |
| Refresh project state | اسکن مجدد پروژه |

### `presentation/commands/`

| فایل | نقش |
|---|---|
| `commands.py` | `Command`، `CommandResult`، `CommandKind` (مجموعه‌ی بسته) |
| `handlers.py` | ۷ handler نازک که به سرویس‌ها وصل می‌شوند |
| `bus.py` | صف، اجرای پس‌زمینه، تاریخچه |

### محافظت‌ها

| مسئله | راه‌حل |
|---|---|
| کار طولانی UI را قفل می‌کند | اجرا در thread پس‌زمینه؛ پاسخ ۲۰۲ فوری |
| دو ingest هم‌زمان روی یک دیتاست | فقط یک دستور در لحظه؛ بقیه `REJECTED` |
| handler خطا بدهد و سرور بمیرد | هر استثنا به `CommandResult.failure` تبدیل می‌شود |
| refresh دستور را دوباره اجرا کند | الگوی Post/Redirect/Get (303) |
| GUI عملیات جدید اختراع کند | `CommandKind` بسته؛ ناشناخته → ۴۰۰ |
| کسی viewer محض بخواهد | `serve --read-only` همه‌ی دکمه‌ها را برمی‌دارد |

### مرزی که هنوز برقرار است

- `DashboardGateway` (مسیر خواندن) هیچ متد تغییردهنده ندارد
- ViewModelها `frozen` و بدون ایمپورت domain/infrastructure
- handlerها فقط delegate می‌کنند — تستی helperهای محاسباتی را رد می‌کند
- تنها endpoint عملیاتی `POST /run` است؛ `PUT`/`DELETE`/`PATCH` → ۴۰۵

### نتیجه‌ی واقعی

```
POST /run  command=run_trading_cycle
  prediction : 0.2448
  signal     : sell
  decision   : enter
  filled     : 0.01 @ 2001.6
  position   : XAUUSD_i SHORT 0.01 @ 2001.6
```

پوزیشن در دیتابیس نوشته شد و بلافاصله روی داشبورد ظاهر شد.

### تست‌ها

۵۹۴ → **۶۳۴** (۴۰ تست جدید: ۱۸ تست command layer، ۱۸ تست HTTP، بقیه رگرسیون)

---

## مرحله‌ی بعدی

**اتصال persistence به بقیه‌ی دموها** — بک‌تست و بهینه‌سازی هنوز پیش‌فرض
حافظه‌ای‌اند. یک فلگ `--persist` باعث می‌شود نتایجشان در دیتابیس بنشیند و
خودکار روی داشبورد ظاهر شود. کوچک است و حلقه را می‌بندد.
